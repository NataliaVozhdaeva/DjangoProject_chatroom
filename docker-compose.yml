version: '3.9'

services:
  web:
    build: .
    container_name: drf-api
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      DEBUG: "True"

#services:
#  db:
#    image: postgres:15
#    environment:
#      POSTGRES_DB: chatroom_db
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: postgres
#    volumes:
#      - postgres_data:/var/lib/postgresql/data
#    ports:
#      - "5432:5432"
#
#  web:
#    build: .
#    command: python manage.py runserver 0.0.0.0:8000
#    volumes:
#      - .:/app
#    ports:
#      - "8000:8000"
#    depends_on:
#      - db
#    environment:
#      - DEBUG=1
#      - DATABASE_URL=postgresql://postgres:postgres@db:5432/chatroom_db
#
#volumes:
#  postgres_data:
